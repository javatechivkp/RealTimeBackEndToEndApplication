spring:
  application:
    name: SpringbootAwsCICDPipelineApp
  datasource:
   #driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://srinutechidb.cf6gogo4kg32.ap-south-1.rds.amazonaws.com:3306/realtimebackend?useSSL=false&allowPublicKeyRetrieval=true
    password: mysqlroot
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.gmail.com   # or smtp.office365.com
    port: 587
    username: srinuvasrao966@gmail.com
    password: tccl vnxy jwsv ayhf  # Use Gmail App Password, not regular password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            protocols: TLSv1.2
            trust: smtp.gmail.com

# Uncomment and set active profile if needed
#  profiles:
#    active:
#      - test


   
logging:
  file:
    name: C:/Users/HI/OneDrive/Desktop/logs/springbootapplication.log

management:
  tracing:
    sampling:
      probability: 1.0